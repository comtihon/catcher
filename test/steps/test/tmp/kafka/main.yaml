---
            steps:
                - kafka: 
                    consume: 
                        server: '127.0.0.1:9092'
                        topic: 'test'
                        where: 
                            equals: {the: '{{ MESSAGE.id }}', is: 'uuid3'}
                    register: {user: '{{ OUTPUT }}'}
                - check:
                    equals: {the: '{{ user.name }}', is: 'bar'}
            